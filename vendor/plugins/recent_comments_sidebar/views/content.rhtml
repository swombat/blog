<h3><%=h title %></h3>
<div>
    <ul>
        <% sidebar.comments.each do |comment| -%>
            <li><i><%= comment.author if show_username -%></i> on <%= link_to_permalink comment, h(comment.article.title) %></li>
        <% end %>
    </ul>
</div>
